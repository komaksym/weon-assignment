name: Execute frozen holdouts once

on:
  pull_request:
    branches:
      - feature/slice-4-holdout-submission

jobs:
  run:
    # One-use caller; close without merging after artifact inspection.
    uses: ./.github/workflows/run-holdout.yml
    with:
      confirm_paid_run: true
    secrets: inherit
